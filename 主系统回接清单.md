# 主系统回接清单

> 日期：2026-05-29  
> 当前目录：`/Users/yukii/Desktop/事理图谱`  
> 主系统目录：`/Users/yukii/Desktop/Kimi_Agent_AI Scratch功能升级`  
> 对应实施方案任务：任务 4，形成主系统接回清单。

## 1. 回接前置检查

已完成检查：

- 当前目录 Git 状态：`main...origin/main`，远程为 `origin https://github.com/lostmyukii/-1.git`。
- 当前目录原型测试：`python3 -m unittest discover -s tests -v` 已通过。
- 主系统目录状态：`/Users/yukii/Desktop/Kimi_Agent_AI Scratch功能升级` 已初始化 Git 仓库。
- 主系统远程：`origin https://github.com/lostmyukii/Kimi_Agent_AI-Scratch.git`。
- 主系统首个提交：`4677147 chore: add safe gitignore`，已推送到 `origin/main`。
- 已新增只读预检工具：`python3 -m event_graph_dev.git_preflight '/Users/yukii/Desktop/Kimi_Agent_AI Scratch功能升级'`。
- 本轮用户已确认可直接在主系统落增量代码，当前工作分支为 `feat/event-graph-positioning`。
- 当前剩余阻塞：服务器地址、部署目录、服务名和回滚方式尚未确认。
- 预检发现 110 个高风险误提交路径，代表项包括 `backend/.env`、`data/vsle_growth_map.db`、`node_modules`、`web/node_modules`、`backend/__pycache__`、`.DS_Store`。
- 已通过 `git check-ignore` 验证代表性高风险路径会被主系统 `.gitignore` 排除。

因此本清单已进入任务 5 的第二阶段业务回接：在第一阶段定位 API 基础上，补齐项目草案生成 API。项目草案只返回 `draft` 项目包骨架和校验预览，不写库、不触发 RAG、不绕过审核入库。

## 2. 原型到主系统文件映射

| 当前目录原型 | 主系统目标文件 | 回接动作 |
| --- | --- | --- |
| `event_graph_dev/models.py` | `backend/app/schemas/event_graph.py` | 将 dataclass 输入输出结构迁移为 Pydantic schema |
| `event_graph_dev/planner.py` | `backend/app/services/event_graph_planner_service.py` | 将纯函数封装为服务函数，保留年龄层、插入位、知识链、风险判断 |
| `event_graph_dev/draft.py` | `backend/app/services/event_graph_draft_export_service.py` 或 `event_graph_planner_service.py` | 将原型导出的 `draft` ProjectPackage 文件夹接入主系统导出/持久化流程 |
| `event_graph_dev/baseline.py` | `backend/app/services/event_graph_baseline_service.py` | 改为使用主系统 SQLAlchemy Session，不在服务中硬编码本地文件路径 |
| `tests/test_planner.py` | `backend/tests/test_event_graph_planner.py` | 迁移 L1/L2/L3 典型定位用例 |
| `tests/test_draft_export.py` | `backend/tests/test_event_graph_draft_export.py` | 迁移草案导出、必备文件、draft 状态和 L1 竞赛边界测试 |
| `tests/test_baseline.py` | `backend/tests/test_event_graph_baseline.py` | 改为主系统测试数据库或只读 fixture |

本轮已落地的主系统增量文件：

- `backend/app/models/event_graph.py`
- `backend/app/schemas/event_graph.py`
- `backend/app/services/event_graph_planner_service.py`
- `backend/app/services/event_graph_draft_export_service.py`
- `backend/app/services/event_graph_generated_draft_service.py`
- `backend/app/api/routes_event_graph.py`
- `backend/app/main.py`
- `backend/tests/test_event_graph_planner.py`
- `backend/tests/test_event_graph_draft_export.py`
- `backend/tests/test_event_graph_generated_drafts.py`

新增 API 文件：

- `backend/app/api/routes_event_graph.py`

需要修改的主系统入口：

- `backend/app/main.py`：引入 `routes_event_graph.router`，并通过 `settings.api_prefix` 注册。

## 3. 第一阶段 API 契约

第一阶段只实现定位接口，不生成项目包。

```text
POST /api/event-graph/ideas/position
POST /api/event-graph/ideas/{idea_id}/generate-project-draft
```

请求体建议：

```json
{
  "idea_text": "我想做一个可以用语音控制的送货小车",
  "age": 8,
  "age_band": null,
  "current_project_id": null,
  "known_knowledge_point_ids": [],
  "available_hardware": ["Arduino", "超声波传感器", "小车底盘"],
  "duration_hours": 8,
  "target_use": "classroom",
  "teacher_constraints": []
}
```

响应体必须包含：

- `stage_decision`：推荐年龄层、插入类型、置信度和理由。
- `knowledge_chain`：前置、核心、后续知识点。
- `insertion_slots`：候选插入位、锚定项目关键词和评分。
- `candidate_existing_project_keywords`：候选项目关键词。
- `generation_recommendation`：复用、变体或新项目包建议。
- `risk_flags`：年龄风险、硬件未知、AI 过度依赖等风险。

第二阶段 `generate-project-draft` 响应必须包含：

- `status=draft`，且 `project_manifest.status=draft`。
- `files` 覆盖 `project_plugin_service.REQUIRED_PACKAGE_FILES` 中所有必备文件。
- `project_manifest` 覆盖 `project_plugin_service.REQUIRED_PROJECT_FIELDS` 中所有必填字段。
- `package_files` 包含 `project.json`、`curriculum.md`、`lesson_plan.json`、`knowledge_map.json`、`materials.json`、`assessment.json`、`ai_guardrails.json`、`zongping_map.json`、`competition_map.json`、`parent_view.md`、`teacher_notes.md`、`README.md`。
- `validation_preview.can_activate=false`，并列出教师确认、教研审核和项目插件校验。
- L1 竞赛想法仍保持前置补齐，不生成 `target_competitions`，不进入竞赛包装。

## 4. 主系统实现顺序

进入任务 5 后按以下顺序执行，每一步测试通过后单独 commit 并 push：

1. 新增 `backend/app/schemas/event_graph.py`。
2. 新增 `backend/app/services/event_graph_planner_service.py`。
3. 新增 `backend/app/api/routes_event_graph.py`。
4. 修改 `backend/app/main.py` 注册 router。
5. 新增 `backend/tests/test_event_graph_planner.py`。
6. 运行主系统后端测试。

第二阶段继续复用同一组主系统文件，补充：

1. `backend/app/schemas/event_graph.py`：新增 `EventGraphProjectDraftRequest` 和 `EventGraphProjectDraftResponse`。
2. `backend/app/services/event_graph_planner_service.py`：新增 `generate_project_draft`，复用项目插件必填字段、必备文件、七维评价常量。
3. `backend/app/api/routes_event_graph.py`：新增 `POST /event-graph/ideas/{idea_id}/generate-project-draft`。
4. `backend/tests/test_event_graph_planner.py`：覆盖完整项目包文件、AI 防认知卸载、七维评价、L1 竞赛边界和 API route 注册。

当前目录原型已额外补齐 `event_graph_dev/draft.py`，能把草案导出为 ProjectPackage 文件夹和 `event_graph_export.json` 清单。回接主系统时需要决定：

- 只导出文件：新增导出 service/API，返回下载路径或打包结果。（已完成第一版）
- 持久化草案：新增 `generated_project_drafts`，保存 `draft_payload_json` 和 `validation_summary_json`。

两种方式都必须保持 `draft`，不得绕过 ProjectPackage validate/review/approve/activate。

主系统导出文件切片已落地：

- `backend/app/services/event_graph_draft_export_service.py`：将 `generate_project_draft` 输出写入 `outputs/event_graph_drafts/{package_id}`。
- `backend/app/api/routes_event_graph.py`：新增 `POST /api/event-graph/ideas/{idea_id}/export-project-draft`。
- `backend/tests/test_event_graph_draft_export.py`：覆盖导出文件、`draft` 状态、`event_graph_export.json`、L1 竞赛边界和 route 注册。

主系统持久化草案切片已落地：

- `backend/app/models/event_graph.py`：新增 `generated_project_drafts`，保存草案生命周期、导出文件血缘、请求、定位结果、`draft_payload_json`、`validation_summary_json`。
- `backend/app/services/event_graph_generated_draft_service.py`：生成草案、复用导出写入函数、持久化记录，并提供查询列表和详情。
- `backend/app/api/routes_event_graph.py`：新增 `POST /api/event-graph/ideas/{idea_id}/generated-project-drafts`、`GET /api/event-graph/generated-project-drafts`、`GET /api/event-graph/generated-project-drafts/{draft_id}`。
- `backend/tests/test_event_graph_generated_drafts.py`：覆盖持久化记录、导出路径、L1 竞赛边界、route 注册，并验证不会创建 `ProjectPackage` 或 `ProjectRAGIndexJob`。

本切片没有复用 `graph_edges.edge_metadata`，因为生成草案不是单条图谱边的说明，而是可审核、可查询、可回滚的业务记录。它仍只保存 `draft`，不会写入 `project_packages`，不会触发 RAG，也不会绕过 ProjectPackage validate/review/approve/activate。

## 5. 测试矩阵

最低测试集：

```bash
cd '/Users/yukii/Desktop/Kimi_Agent_AI Scratch功能升级/backend'
python3 -m pytest tests/test_event_graph_planner.py -v
python3 -m pytest tests/test_project_alignment_normalizer.py tests/test_project_ai_knowledge_expander.py -v
```

定位用例必须覆盖：

- L1 竞赛想法只能进入前置补齐，不直接推荐竞赛路径。
- L2 语音送货小车应倾向同 ProjectSet 变体。
- L3 数据/AI 想法应进入系统设计和数据统计链路。
- “直接生成完整代码”必须触发 AI 过度依赖风险。
- 项目草案生成必须只返回 `draft`，不得直接 active。
- 项目草案必须覆盖项目插件必备文件和七维评价。
- L1 竞赛想法不得生成竞赛目标。

## 6. 不允许越过的边界

- 不允许绕过 ProjectPackage 审核流程直接把生成项目设为 `active`。
- 不允许在“一句话备课”中直接生成学生可复制的完整代码。
- 不允许让 L1 直接进入竞赛包装路径。
- 不允许在主系统未配置 Git remote 时进入服务器更新阶段。
- 不允许把本地未提交文件直接复制到服务器。

## 7. 当前阻塞与下一步

当前阻塞：

- 主系统远程分支、服务器地址、部署目录、服务名和回滚方式尚未确认。

下一步：

1. 实现教研审核记录切片：新增 `project_graph_review_records`，记录教师确认、教研审核、系统校验意见和状态流转。
2. 在审核记录通过后，再接入现有 ProjectPackage validate/submit-review/approve 流程，但仍不得直接 `active`。
3. 服务器地址、部署目录、服务名和回滚方式确认前，不进入更新服务器阶段。
