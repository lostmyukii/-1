# 事理图谱开发实施方案

> 日期：2026-05-29  
> 工作目录：`/Users/yukii/Desktop/事理图谱`  
> 主系统目录：`/Users/yukii/Desktop/Kimi_Agent_AI Scratch功能升级`  
> 核心流程：当前目录独立开发与验证 -> 回接主系统 -> 推送 Git -> 更新服务器。

## 1. 总体开发策略

本功能默认不直接在主系统里边想边改。先在当前目录形成“可跑、可测、可解释”的轻量原型，再把稳定接口迁移到主系统。用户明确要求在 `/Users/yukii/Desktop/Kimi_Agent_AI Scratch功能升级` 落代码时，按功能分支和增量文件策略执行。

采用三层推进：

1. 原型层：当前目录 `event_graph_dev/`，只读读取主系统数据库和项目 JSON，完成教师思路解析、图谱定位、插入位评分和项目草案结构。
2. 集成层：把原型中稳定的模型、服务、测试迁回主系统 `backend/app/` 和 `backend/tests/`。本轮采用增量回接，不提交历史文档、数据库备份、视频产物、本地依赖和缓存。
3. 部署层：主系统代码推送到远程后，由服务器拉取指定提交或 tag，完成迁移、重启和健康检查。

这样做的原因：

- 当前目录可以快速实验，不污染主系统。
- 主系统集成时接口已经稳定，风险更小。
- 服务器更新只基于已推送的 Git 提交，便于回滚。

## 2. 当前目录代码结构

```text
/Users/yukii/Desktop/事理图谱
├── AGENTS.md
├── 事理图谱设计方案.md
├── 事理图谱开发实施方案.md
├── 主系统回接清单.md
├── event_graph_dev/
│   ├── __init__.py
│   ├── baseline.py
│   ├── draft.py
│   ├── git_preflight.py
│   ├── models.py
│   └── planner.py
└── tests/
    ├── __init__.py
    ├── test_baseline.py
    ├── test_draft_export.py
    ├── test_git_preflight.py
    └── test_planner.py
```

职责：

| 文件 | 职责 |
| --- | --- |
| `event_graph_dev/models.py` | 定义教师思路、知识链、插入位、推荐决策等纯数据结构 |
| `event_graph_dev/baseline.py` | 只读读取主系统数据库和项目 JSON，形成项目/知识/边的基线摘要 |
| `event_graph_dev/draft.py` | 生成并导出 `draft` ProjectPackage 草案文件夹，不写库、不激活 |
| `event_graph_dev/git_preflight.py` | 只读检查主系统 Git 仓库、remote 和高风险误提交路径 |
| `event_graph_dev/planner.py` | 基于教师一句话、年龄、硬件和目标用途，生成阶段判断、知识链和插入位建议 |
| `tests/test_baseline.py` | 验证主系统事实源可读取，关键数量不会意外为空 |
| `tests/test_draft_export.py` | 验证草案导出覆盖项目包必备文件、draft 状态、L1 竞赛边界和审核门 |
| `tests/test_git_preflight.py` | 验证主系统 Git 预检能识别缺失 remote 和风险路径 |
| `tests/test_planner.py` | 验证 L1/L2/L3 典型教师思路的定位、风险和生成模式 |

## 3. 当前目录开发任务

### 任务 1：建立原型数据模型

目标：用 dataclass 固定“教师思路 -> 知识链 -> 插入位 -> 生成建议”的接口。

输出：

- `TeacherIdea`
- `StageDecision`
- `KnowledgeChain`
- `InsertionSlot`
- `GenerationRecommendation`
- `PositioningResult`

验证：

```bash
python3 -m unittest tests.test_planner -v
```

Git：

```bash
git add event_graph_dev/models.py tests/test_planner.py
git commit -m "feat: add event graph planning models"
git push
```

如果当前目录没有 remote，`git push` 会被阻塞，需要先配置远程。

### 任务 2：建立主系统基线读取器

目标：只读读取 `vsle_growth_map.db` 和 `wsl_160_projects.json`，输出项目数、年龄分布、图谱关系分布和插件池分布。

验证：

```bash
python3 -m unittest tests.test_baseline -v
```

Git：

```bash
git add event_graph_dev/baseline.py tests/test_baseline.py
git commit -m "feat: add event graph baseline reader"
git push
```

### 任务 3：实现教师思路定位原型

目标：输入一句话、年龄段、硬件和目标用途后，输出：

- 推荐年龄层。
- 插入类型。
- 前置、核心、后续知识点。
- 候选项目关键词。
- 生成模式：复用、变体或新项目包。
- 风险标记。

验证：

```bash
python3 -m unittest tests.test_planner -v
```

Git：

```bash
git add event_graph_dev/planner.py tests/test_planner.py
git commit -m "feat: add event graph idea planner"
git push
```

### 任务 3.5：实现项目草案导出原型

目标：对需要生成或变体化的教师思路，生成可审核的 `draft` ProjectPackage 草案目录。

输出：

- `ProjectDraft`
- `DraftExportResult`
- 12 个项目包必备文件。
- `event_graph_export.json` 导出清单，包含定位结果、校验预览和状态流转。

边界：

- 只导出文件，不写入主系统数据库。
- `project.json.status` 和 `lifecycle_stage` 固定为 `draft`。
- `rag_indexable=false`，`recommendable=false`。
- L1 竞赛想法不生成 `target_competitions`。

验证：

```bash
python3 -m unittest tests.test_draft_export -v
```

Git：

```bash
git add event_graph_dev/draft.py event_graph_dev/__init__.py tests/test_draft_export.py 事理图谱开发实施方案.md 主系统回接清单.md 事理图谱设计方案.md
git commit -m "feat: export event graph project drafts"
git push
```

### 任务 4：形成主系统接回清单

目标：把当前目录原型映射到主系统正式文件。

回接目标：

| 当前目录原型 | 主系统目标 |
| --- | --- |
| `event_graph_dev/models.py` | `backend/app/schemas/event_graph.py` |
| `event_graph_dev/baseline.py` | `backend/app/services/event_graph_baseline_service.py` 或管理脚本 |
| `event_graph_dev/planner.py` | `backend/app/services/event_graph_planner_service.py` |
| `tests/test_planner.py` | `backend/tests/test_event_graph_planner.py` |
| `tests/test_baseline.py` | `backend/tests/test_event_graph_baseline.py` |

详细文件映射、API 契约、测试矩阵和当前阻塞记录在 `主系统回接清单.md`。

Git：

```bash
git add 事理图谱开发实施方案.md AGENTS.md
git commit -m "docs: add event graph development workflow"
git push
```

### 任务 5：回接主系统

前置：

- 当前目录测试通过。
- 当前目录变更已 commit。
- 当前目录变更已 push，或已明确记录无 remote 的阻塞。
- 主系统目录已初始化 Git 仓库并配置 remote。
- 用户已确认本轮可在主系统落增量代码，不等待完整历史基线。

主系统分支：

```bash
git switch -c feat/event-graph-positioning
```

回接顺序：

1. 新增 schema：`backend/app/schemas/event_graph.py`。
2. 新增 service：`backend/app/services/event_graph_planner_service.py`。
3. 新增 API route：`backend/app/api/routes_event_graph.py`。
4. 接入 `backend/app/main.py` router。
5. 新增 tests：`backend/tests/test_event_graph_planner.py`。
6. 跑后端测试。
7. commit + push。

测试建议：

```bash
cd '/Users/yukii/Desktop/Kimi_Agent_AI Scratch功能升级/backend'
python3 -m unittest backend.tests.test_event_graph_planner -v
python3 -m unittest backend.tests.test_project_alignment_normalizer backend.tests.test_project_ai_knowledge_expander -v
```

当前第一阶段已实现 `POST /api/event-graph/ideas/position` 对应的 schema、service、route、router 注册和单元测试。`pytest` 在系统 Python 中不可用时，使用 `unittest` 作为验证命令。

当前第二阶段已实现：

- `POST /api/event-graph/ideas/{idea_id}/generate-project-draft`：即时返回 `draft` 项目包骨架。
- `POST /api/event-graph/ideas/{idea_id}/export-project-draft`：导出 `draft` ProjectPackage 文件夹到 `outputs/event_graph_drafts/{package_id}`。
- `backend/tests/test_event_graph_planner.py` 和 `backend/tests/test_event_graph_draft_export.py` 覆盖定位、草案生成、导出文件、L1 竞赛边界、AI 防认知卸载和七维评价。

### 任务 6：服务器更新

服务器更新不能从当前目录直接更新。必须先完成主系统回接、测试、commit 和 push。

上线前检查：

- 远程分支存在。
- CI 或本地测试通过。
- 数据库已备份。
- 已确认部署路径和服务名。
- 已准备回滚 tag 或上一稳定 commit。

部署顺序：

```bash
git fetch --all --tags
git checkout <release-branch-or-tag>
python3 -m pytest backend/tests/test_event_graph_planner.py -v
systemctl restart <backend-service>
curl -f <server-health-url>
```

具体命令要根据服务器部署方式补全。没有服务器地址、服务名和部署目录时，不执行更新服务器。

## 4. Git 推送规则

用户要求“每一步推送 git”，执行规则如下：

1. 每个任务完成一个可验证小步。
2. 先跑测试。
3. 再 commit。
4. 立刻 push。
5. push 失败时停止并记录原因。

当前状态与阻塞：

- 当前目录已初始化为本地 Git 仓库。
- 当前目录已有首次提交：`b164adb chore: initialize event graph workspace`。
- 当前目录已配置远程：`origin https://github.com/lostmyukii/-1.git`。
- 主系统目录已初始化为 Git 仓库。
- 主系统目录已配置远程：`origin https://github.com/lostmyukii/Kimi_Agent_AI-Scratch.git`。
- 主系统目录已推送首个提交：`4677147 chore: add safe gitignore`。
- 主系统采用增量回接策略，当前功能分支只提交事理图谱定位 API 相关文件。

因此当前目录和主系统目录都可以按“测试 -> commit -> push”闭环推进；更新服务器前，还需要确认服务器部署入口、数据库备份方式和回滚方式。

## 5. 回接后的主系统接口建议

新增 API：

```text
POST /api/event-graph/ideas/position
POST /api/event-graph/ideas/{idea_id}/generate-project-draft
GET  /api/event-graph/ideas/{idea_id}
GET  /api/event-graph/slots/{slot_id}
```

第一阶段已实现 `position`，先完成教师思路定位闭环。第二阶段已补充 `generate-project-draft` 和当前目录 `draft` 项目包导出原型，只生成 `draft` 项目包骨架和校验预览，不写库、不触发 RAG、不绕过 ProjectPackage 审核流程。

## 6. 本阶段完成标准

当前目录完成标准：

- `AGENTS.md` 包含独立开发、回接、服务器更新和 Git 推送流程。
- `事理图谱开发实施方案.md` 存在并可作为执行计划。
- `event_graph_dev/` 有可运行原型。
- `event_graph_dev/draft.py` 能导出可审核的 `draft` 项目包文件夹。
- `tests/` 有基础测试。
- `python3 -m unittest discover -s tests -v` 通过。

主系统回接完成标准：

- 主系统新增 event graph planner service、schema、route 和测试。
- 不破坏现有项目推荐和项目插件机制。
- 新接口能返回阶段、知识链、插入位和生成建议。
- 项目草案生成接口只返回 `draft`，覆盖项目插件必备文件、必填字段、AI 防认知卸载和七维评价边界。
- 项目草案导出接口只写入 `outputs/event_graph_drafts`，不写入数据库、不激活、不触发 RAG。
- 变更已 commit 并 push。

服务器更新完成标准：

- 服务器拉取的是已推送 commit 或 tag。
- 健康检查通过。
- 关键接口 smoke test 通过。
- 有清晰回滚点。
